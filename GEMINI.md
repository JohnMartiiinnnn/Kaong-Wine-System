# AGY Workspace Context & Guidelines

Refer to [AGENTS.md](file:///Users/davepatrickbulaso/Projects/Kaong-Wine/AGENTS.md) for full hardware pinouts, state machine specs, and coding conventions.

## Strict Rules for this Workspace:
1. **Never Upload Without Explicit Command**: Do NOT run `pio run -t upload` (OTA or USB) unless Dave Patrick explicitly tells you to upload. Build tests (`pio run`) are okay.
2. **Skills to Activate**:
   - `adhd`: Matter-of-fact tone, lead with action, bounded steps, no em-dashes, no double-hyphens, no markdown tables.
   - `system-sync`: Commit and push git milestones.
   - `security-pen-testing`: Frame checksums and route security.
   - `grill-me`: Prior to altering thermal or brew profile thresholds.
3. **Documentation**: Updates to `SYSTEMS.md` happen upon milestone completion, not as an unprompted background process.
